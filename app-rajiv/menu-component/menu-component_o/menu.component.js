'use strict';

// Register `menuPage` component, along with its associated controller and template
angular.
  module('menuPage').
  component('menuPage', {
    templateUrl: 'menu-component/menu.template.html',
    controller: ['$http', function HomeController($http) {
    }]
  });
